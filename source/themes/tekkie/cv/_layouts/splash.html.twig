<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <meta name="description" content="{{ site.description }}">
    <meta name="author" content="{{ site.author }}">
    <link rel="icon" href="../../favicon.ico">

    <title>{% block title %}{% if 'Home' != page.title %}{{ page.title }} &mdash; {{ site.title }}{% else %}{{ site.title }} &mdash; {{ site.subtitle }}{% endif %}{% endblock %}</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.3/css/font-awesome.min.css">

    <!-- Custom styles for this template -->
    {% for stylesheet in theme_paths("themes/tekkie/cv/assets/css/style.css") %}
    <link href="{{ site.url }}/{{ stylesheet }}" rel="stylesheet" media="screen" />
    {% endfor %}
    {% for stylesheet in theme_paths("themes/tekkie/cv/assets/css/flag-icon.css") %}
    <link href="{{ site.url }}/{{ stylesheet }}" rel="stylesheet" media="screen" />
    {% endfor %}

{% block head_styles %}
{% endblock %}
{% block head_scripts %}
{% endblock %}
  </head>

  <body{% block body_id %}{% endblock %}{% block body_class %}{% endblock %}>
{% include "body-start" %}
{% include "navigation" %}

    <div class="section">
      <div class="container">
        <div class="cover-container">

{% block content_wrapper %}{% block content %}{% endblock %}{% endblock %}

        </div>
      </div>
    </div>

{% include "footer-content" %}
{% include "footer-js" %}
  </body>
</html>
